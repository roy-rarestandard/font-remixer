{
  "name": "codex-font-remixer",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test tests/*.test.cjs",
    "qa": "npm run test"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "typescript": "^5.6.3"
  }
}
